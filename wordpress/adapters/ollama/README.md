# Ollama Adapter

Create one Modelfile per skill and place the selected `SKILL.md` body in the `SYSTEM` block. Choose the base model separately per deployment.

Ollama prompt-only use is suitable for planning, review, design direction, and implementation guidance. It cannot perform WordPress or Elementor actions unless wrapped by an app that exposes those tools.
