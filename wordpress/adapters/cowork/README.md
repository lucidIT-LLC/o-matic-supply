# Cowork MCP Adapter

Use the same MCP server entry points with explicit environment variables or profile config:

- WordPress: `server/index.mjs`
- Elementor: `server/elementor.mjs`

The host must provide project credentials or point the connector to a project-local profile file. Skills can be loaded separately as system/developer instructions.
