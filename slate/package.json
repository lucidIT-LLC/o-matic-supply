{
  "name": "o-matic-slate-api",
  "version": "0.1.0",
  "private": true,
  "description": "o-MATIC Supply connector for the governed Slate API published by O-Matic Server.",
  "type": "module",
  "engines": {"node": ">=20"},
  "license": "BUSL-1.1",
  "bin": {"o-matic-slate-api": "dist/index.mjs"},
  "scripts": {
    "check": "node --check server/index.mjs && node server/index.mjs --self-test",
    "build": "esbuild server/index.mjs --bundle --minify --platform=node --format=esm --outfile=dist/index.mjs",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
